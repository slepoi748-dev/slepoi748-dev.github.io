// package.json
{
  "name": "lockscreen-prop",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint src",
    "test": "vitest run"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "vite": "^5.0.0",
    "vitest": "^2.0.0"
  }
}